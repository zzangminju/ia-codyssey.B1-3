# ia-codyssey.B1-3

코드세션 자동화 프로젝트 저장소입니다.

## 📑 보고서

### 프로젝트 1 — 자동화 도구 비교 분석
학원 대기 신청 DB 자동화를 **Make와 Zapier**로 각각 구현하고 두 도구를 비교 분석한 보고서입니다.

- 📄 [프로젝트1 자동화도구 비교분석 보고서 (Markdown)](프로젝트1_자동화도구_비교분석_보고서.md)
- 🗂️ [비교분석 보고서 (이미지 포함 ZIP)](프로젝트1_자동화도구_비교분석_보고서_이미지포함.zip)

### 프로젝트 2 — 자유 주제 자동화 설계 및 구현
BBC 영어 뉴스 기반 **맞춤형 시사영어 학습 자동화 시스템** (Make · OpenAI · Notion · Gmail 연동) 보고서입니다.

- 📄 [자동화 프로젝트 최종 보고서 (Markdown)](자동화프로젝트_보고서_최종.md)
- 📝 [자동화 프로젝트 최종 보고서 (Word)](자동화프로젝트_보고서_최종.docx)

# [프로젝트 1] 자동화 도구 비교 분석 보고서

## 학원 대기 신청 DB 자동화 — Make vs Zapier

## 1. 개요

### 1.1 자동화 대상 업무

학원 입학 대기 신청을 구글 폼으로 받은 뒤, 신청자를 **N수생 / 재학생**으로 분류해 각각의 시트 탭에 정리하고, 원장에게 신규 신청 알림 메일을 보내는 반복 업무를 자동화했다. 기존에는 폼 응답을 사람이 직접 확인해 분류·기록·알림을 처리해야 했으나, 이를 트리거-분기-액션 구조로 자동화했다.

### 1.2 사용한 도구

동일한 워크플로우를 아래 두 도구로 각각 구현했다.

| 구분 | 도구 |
| --- | --- |
| 도구 1 | Make (구 Integromat) |
| 도구 2 | Zapier |

## 2. 공통 워크플로우 구조

두 도구 모두 아래의 동일한 논리 구조로 구현했다.

```
[Trigger]  구글 폼 제출 → 응답 시트에 새 행 추가 감지
     │
[분기]     N수생 / 재학생 여부 검사
     ├─ N수생  → 시트 N수생 탭에 행 추가 → 원장에게 알림 메일
     └─ 재학생 → 시트 재학생 탭에 행 추가 → 원장에게 알림 메일
```

### 2.1 공통 요구 사항 충족 여부

| 요구 사항 | 충족 내용 | 충족 여부 |
| --- | --- | --- |
| Trigger 1개 이상 | 구글 시트 새 행 감지 (폼 제출 시 자동 생성) | ✅ 1개 |
| Action 2개 이상 | 시트 행 추가 + Gmail 알림 발송 (경로별 2개) | ✅ 다수 |
| 조건 분기 1개 이상 | N수생 / 재학생 분기 | ✅ 1개 |
| 각 분기 경로 1회 이상 실행 | N수생·재학생 각각 테스트 제출로 양쪽 경로 실행 확인 | ✅ 확인 |

### 2.2 데이터 매핑

폼 응답을 시트 열에 아래와 같이 매핑했다.

| 시트 열 | 폼 응답 값 |
| --- | --- |
| 타임스탬프 | 폼 제출 시각 |
| 이름 | 이름 |
| 전화번호 | (폼 수집 항목인 이메일/연락처 값) |
| 특이사항 | 직전 학기 성적 · 현재 교재/진도 · 타 학원 수강 이력 (장문 답변) |
| 입학 희망 날짜 | 입학 희망 날짜 |

## 3. 도구 1 — Make 구현

### 3.1 구현 과정 요약

- **트리거 모듈** — Google Sheets > Watch New Rows로 폼 응답 시트의 새 행을 감지하도록 설정했다.

- **라우터 추가** — 트리거 뒤에 Router 모듈을 붙여 흐름을 두 갈래로 분기했다.

- **경로 A (N수생)** — 라우터 연결선에 필터(N수생/재학생 여부 = N수생)를 설정하고, Google Sheets > Add a Row(N수생 탭)와 Gmail > Send an Email(N수생 알림)을 연결했다.

- **경로 B (재학생)** — 동일한 방식으로 필터(= 재학생)와 시트 행 추가(재학생 탭), Gmail 알림을 연결했다.

- **테스트** — Run once 상태에서 폼을 N수생·재학생으로 각각 제출해 두 경로가 모두 정상 실행됨을 확인했다.

Make는 라우터 하나로 분기 전체를 **한 시나리오 안에** 구성할 수 있었다.

### 3.2 워크플로우 구성 화면

📷 **[캡처 삽입]** Make 시나리오 전체 구성 화면 (트리거 → 라우터 → 두 경로의 시트/메일 모듈)

### 3.3 실행 결과 화면

N수생 경로 실행 결과 (모듈 실행 표시)

 재학생 경로 실행 결과 

시트 N수생 탭 / 재학생 탭에 각각 행이 추가된 화면

원장이 수신한 알림 메일

## 4. 도구 2 — Zapier 구현

### 4.1 구현 과정 요약

Zapier는 분기 기능인 Paths가 유료(Professional) 전용이라, 이를 사용하지 않고 **같은 트리거를 쓰는 Zap 2개 + 필터** 구조로 동일한 분기 동작을 구현했다.

**Zap 1 (N수생용)**

- **트리거** — Google Sheets > New or Updated Spreadsheet Row로 폼 응답 시트 감지.

- **필터** — Filter by Zapier로 N수생/재학생 여부 = N수생일 때만 통과.

- **액션 1** — Google Sheets > Create Spreadsheet Row로 N수생 탭에 행 추가.

- **액션 2** — Gmail > Send Email로 원장에게 N수생 알림 발송.

**Zap 2 (재학생용)**

- Zap 1을 복제(Duplicate)한 뒤, 필터 조건(= 재학생)·시트 탭(재학생)·메일 제목만 변경했다.

테스트 시 N수생 응답은 Zap 1만 통과(Zap 2는 필터에서 중단), 재학생 응답은 그 반대로 동작함을 확인했다.

### 4.2 워크플로우 구성 화면

Zap 1 전체 단계 (트리거 → 필터 → 시트 → Gmail) 

필터 조건 설정 화면 (N수생/재학생 여부 Exactly matches N수생) 

Zap 2 (재학생용) 단계 화면 

Zap 목록 (두 Zap 모두 켜진 상태)

### 4.3 실행 결과 화면

시트 N수생 탭 / 재학생 탭 결과 

원장이 수신한 알림 메일

## 5. 비교 분석

동일한 워크플로우를 구현하면서 관찰한 차이를 7개 항목으로 비교했다.

| 비교 항목 | Make | Zapier |
| --- | --- | --- |
| 1. UI/UX | 캔버스에 모듈을 자유롭게 배치하는 시각적 구조. 전체 흐름이 한눈에 보임 | 위→아래로 쌓이는 단계형 구조. 직선적이고 따라가기 쉬움 |
| 2. 분기 구현 방식 | 라우터 1개로 한 시나리오 안에서 분기 | Paths가 유료라, Zap 2개 + 필터로 우회 구현 |
| 3. 설정 난이도 | 캔버스·라우터 개념에 익숙해질 시간이 필요 (중) | 단계가 순차적이라 처음 다루기 쉬움 (하) |
| 4. 연동 서비스 범위 | 약 1,800개 앱 | 약 7,000개 이상 앱 (업계 최다 수준) |
| 5. 무료 플랜 범위 | 월 1,000 operations, 멀티스텝·라우터·필터 모두 무료 | 월 100 tasks, 무료는 2단계(트리거+액션 1개)만 가능. 분기·멀티스텝은 유료(가입 시 14일 체험) |
| 6. 실행 로그 확인 방식 | 각 모듈 위 실행 표시와 데이터 흐름을 시각적으로 확인 | Zap History 탭에서 실행 내역을 단계별로 확인 |
| 7. 분기 관리 편의성 | 한 시나리오에서 모든 경로를 함께 관리 | 경로마다 Zap이 분리돼, 조건 변경 시 여러 Zap을 따로 수정 |

## 6. 각 도구의 장단점

### 6.1 Make

**장점**

- 라우터로 분기를 **무료**로, 그리고 **하나의 시나리오 안에** 구성할 수 있다.

- 전체 워크플로우가 캔버스에 시각적으로 펼쳐져 구조 파악이 쉽다.

- 무료 플랜에서도 멀티스텝·필터·라우터를 모두 쓸 수 있어 학습·실습에 유리하다.

**단점**

- 캔버스와 라우터 개념이 처음엔 다소 낯설어 진입 장벽이 있다.

- 연동 가능한 앱 수가 Zapier보다 적다.

### 6.2 Zapier

**장점**

- 단계형 구조가 직관적이어서 처음 자동화를 접해도 따라가기 쉽다.

- 연동 가능한 앱 수가 압도적으로 많아 다양한 서비스를 붙이기 좋다.

**단점**

- 핵심 분기 기능인 Paths가 유료(Professional) 전용이라, 무료로는 Zap을 여러 개 만들어 우회해야 한다.

- 무료 플랜이 2단계 Zap으로 제한돼, 이 워크플로우 같은 멀티스텝 자동화는 사실상 유료 플랜(또는 체험판)이 필요하다.

- 경로가 늘면 Zap 수도 함께 늘어 관리 부담이 커진다.

## 7. 어떤 상황에 적합한가 (의견)

**Make가 적합한 경우**

- 비용을 들이지 않고 분기·다단계 워크플로우를 구현하고 싶을 때.

- 분기 경로가 여러 개이거나 앞으로 늘어날 가능성이 있어, 한곳에서 흐름을 관리하고 싶을 때.

- 전체 자동화 구조를 시각적으로 보면서 설계하는 방식을 선호할 때.

이번 학원 대기 신청처럼 분기가 핵심이고 비용 부담을 줄이려는 경우에는 **Make가 더 적합**하다고 판단했다.

**Zapier가 적합한 경우**

- 분기가 거의 없는 단순·선형 자동화를 빠르게 만들고 싶을 때.

- 잘 알려지지 않은 서비스까지 폭넓게 연동해야 할 때 (앱 호환성이 최우선일 때).

- 자동화 도구를 처음 다뤄 직관적인 단계형 인터페이스가 필요할 때.

## 8. 결론

동일한 "학원 대기 신청 분류·알림" 워크플로우를 Make와 Zapier로 각각 구현하며, 두 도구가 **같은 결과를 내면서도 분기 처리 철학이 다르다**는 점을 확인했다. Make는 라우터로 한 시나리오 안에서 분기를 무료로 처리하는 반면, Zapier는 분기를 유료 기능으로 두어 무료 사용자는 Zap 분리로 우회해야 했다.

이번 과제처럼 **조건 분기가 핵심이고 비용을 최소화**하려는 워크플로우에서는 Make가, **연동 범위와 단순함**이 우선인 워크플로우에서는 Zapier가 더 적합하다는 결론을 얻었다.


# 자동화 프로젝트 보고서

## [프로젝트 2] 자유 주제 자동화 설계 및 구현

**BBC 영어 뉴스 기반 맞춤형 시사영어 학습 자동화 시스템**

> Make · OpenAI(GPT) · Notion · Gmail 연동

| 항목 | 내용 |
|------|------|
| **과제명** | [프로젝트 2] 자유 주제 자동화 설계 및 구현 |
| **주제** | 매일 아침 영어 뉴스 학습 습관 형성을 위한 자동화 |
| **사용 도구** | Make (no-code 자동화 플랫폼) |
| **연동 서비스** | BBC RSS · OpenAI GPT-4o · Notion · Gmail |
| **작성일** | 2026. 06. 27 |

---

## 1. 자동화할 반복 업무 정의

### 1.1 배경 및 동기

영어 실력 향상과 시사 상식 습득을 위해 매일 영어 뉴스를 읽는 것은 효과적인 학습 방법이다. 하지만 실제로는 매일 직접 뉴스 사이트에 접속하고, 읽을 만한 기사를 고르고, 모르는 표현을 사전에서 찾고, 예문을 정리하는 과정이 번거로워 학습 습관으로 이어지기 어렵다.

본 프로젝트의 목표는 **'매일 영어 뉴스 학습 습관 만들기'** 이다. 사람이 직접 하던 반복 작업을 자동화하여, 사용자는 매일 아침 도착하는 이메일을 확인하고 학습만 하면 되도록 시스템을 구성한다.

### 1.2 기존 수작업 흐름 (자동화 대상)

1. 매일 아침 BBC 뉴스 사이트에 직접 접속한다.
2. 여러 기사 중 학습에 적합한 난이도의 기사를 직접 고른다.
3. 기사를 읽으며 모르는 고급 표현을 추려낸다.
4. 각 표현의 뜻과 영영 정의, 예문을 사전에서 찾아 정리한다.
5. 정리한 내용을 노트(노션 등)에 옮겨 적는다.
6. 복습을 위한 퀴즈를 직접 만든다.

위 6단계는 매일 반복되며, 한 번 수행에 **20~30분**이 소요되는 전형적인 반복 업무이다. 이 전 과정을 자동화 대상으로 정의한다.

---

## 2. 자동화 도구 선정 및 선정 이유

### 2.1 선정 도구: Make

자동화 도구로 **Make(구 Integromat)** 를 선정하였다. Make는 여러 웹 서비스를 시각적으로 연결하여 워크플로우를 구성하는 노코드(no-code) 자동화 플랫폼이다.

### 2.2 선정 이유

- **노코드 시각적 구성:** 코딩 없이 모듈을 드래그·연결하는 방식으로, 워크플로우 전체 흐름을 한눈에 보며 설계할 수 있다.
- **풍부한 연동 모듈:** RSS, OpenAI, Notion, Gmail 등 본 프로젝트에 필요한 서비스가 모두 기본 모듈로 제공되어 별도 개발이 필요 없다.
- **Router를 통한 조건 분기:** Router와 Filter 기능으로 조건에 따라 데이터 흐름을 분기시킬 수 있어, 난이도 기반 필터링 요구사항을 충족한다.
- **스케줄 자동 실행:** 지정한 시각에 자동으로 워크플로우를 실행하는 스케줄링을 기본 지원하여, 매일 아침 자동 실행이 가능하다.
- **AI 연동 용이성:** OpenAI 모듈을 통해 GPT를 워크플로우 중간에 자연스럽게 삽입할 수 있어, 난이도 분류와 학습 콘텐츠 생성에 AI를 활용할 수 있다.

---

## 3. 워크플로우 설계 문서

### 3.1 전체 구조 다이어그램

전체 워크플로우는 1개의 트리거와 다수의 액션, 그리고 1개의 조건 분기(Router)로 구성된다. Router는 메인 경로와 fallback 경로로 나뉘며, 두 경로 모두 실제로 동작한다.

```
① BBC RSS (Trigger)
   ↓ 새 뉴스 헤드라인 수집
② OpenAI GPT — 난이도 분류 (Action)
   ↓ High / Medium / Low 태깅
③ Router — 조건 분기 (Filter)
   ├─ 메인 경로: Medium · High 통과
   │     ↓
   │   ④ GPT 학습 콘텐츠 생성 → ⑤ Notion 저장 → ⑥ Gmail 학습 알림
   └─ fallback 경로: 그 외(Low)
         ↓
       Gmail 스킵 알림 발송
```

### 3.2 단계별 흐름 설명

**① BBC RSS 수집 (Trigger)**

매일 지정된 시각에 BBC World 뉴스 RSS를 조회하여 새로 올라온 최신 헤드라인을 자동으로 가져온다. 이 모듈이 전체 워크플로우를 시작시키는 유일한 트리거이다.

**② 난이도 분류 (Action)**

수집한 뉴스 텍스트를 첫 번째 GPT에 전달한다. GPT는 어휘의 격식과 문장 구조의 복잡성을 분석하여 High / Medium / Low 중 하나로 난이도를 태깅한다.

**③ 조건 분기 (Filter / Router)**

Router를 사용해 난이도에 따라 흐름을 둘로 나눈다. 학습 가치가 있는 Medium·High 뉴스는 메인 경로로 통과시키고, 통과 조건에 맞지 않는(Low) 뉴스는 fallback(예비) 경로로 보낸다.

**④-A 학습 콘텐츠 생성 (Action)**

메인 경로로 통과한 뉴스를 두 번째 GPT에 전달한다. 뉴스 요약, 고급 표현 5개(뜻·영영 정의·예문), 복습용 객관식 퀴즈 1문제로 구성된 학습 콘텐츠를 생성한다.

**⑤-A Notion 저장 (Action)**

생성된 콘텐츠를 Notion 단어장 데이터베이스에 저장한다. 날짜·뉴스 제목·난이도·학습 완료 속성을 채운 새 페이지를 만들고 본문에 학습 내용을 넣는다.

**⑥-A Gmail 학습 알림 발송 (Action)**

저장이 끝나면 학습 콘텐츠를 이메일로 발송한다. 사용자는 매일 아침 이 메일을 받아 학습을 시작한다.

**④-B Gmail 스킵 알림 발송 (Action / fallback 경로)**

Medium·High 뉴스가 한 건도 없는 날에는 fallback 경로가 실행되어, '오늘은 학습에 적합한 뉴스가 없다'는 안내 메일을 발송한다. 이로써 어떤 경우에도 사용자에게 결과를 알린다.

### 3.3 조건 분기(Router) 동작 방식

Router의 메인 경로에는 `Content가 Low를 포함하지 않음(Does not contain Low)` 필터를 설정하여, Medium·High 난이도의 뉴스만 통과시킨다. 나머지 경로는 fallback(예비) 경로로 지정하였다. fallback 경로는 메인 경로를 통과하지 못한 데이터, 즉 Low 난이도 뉴스가 흘러갈 때만 실행된다.

이 구조 덕분에:
- **학습 가치가 있는 뉴스가 있는 날** → 메인 경로(콘텐츠 생성·저장·학습 알림) 실행
- **적합한 뉴스가 없는 날** → fallback 경로(스킵 알림) 실행

### 3.4 요구사항 충족 매핑

| 요구사항 | 본 프로젝트에서의 구현 |
|----------|----------------------|
| **Trigger 1개 이상** | ① BBC RSS 'Watch RSS feed items' (1개) |
| **Action 2개 이상** | ② GPT 난이도분류, ④ GPT 콘텐츠생성, ⑤ Notion 저장, ⑥ Gmail 학습알림, ④-B Gmail 스킵알림 (총 5개) |
| **조건 분기 1개 이상** | ③ Router (메인 경로 / fallback 경로, 1개) |
| **각 분기 경로 1회 이상 실행** | 메인 경로·fallback 경로 모두 실행 결과 확인 (5장 참조) |
| **자동 실행 구조** | 매일 오전 7:30 스케줄 자동 실행 |

---

## 4. 구현 화면 캡처

Make 시나리오 편집기에서 각 모듈을 설정한 화면이다.

### 4.1 전체 시나리오 구성

<!-- 이미지: images/4-1_전체시나리오.png -->
![전체 시나리오 구성](images/4-1_전체시나리오.png)

### 4.2 ① RSS 트리거 설정

BBC World 뉴스 RSS 주소(`https://feeds.bbci.co.uk/news/world/rss.xml`)를 등록하고, 한 번에 가져올 기사 수를 설정하였다.

<!-- 이미지: images/4-2_RSS트리거.png -->
![RSS 트리거 설정](images/4-2_RSS트리거.png)

### 4.3 ② 난이도 분류 GPT 설정

System 메시지로 난이도를 High/Medium/Low 한 단어로만 응답하도록 지시하고, User 메시지에 RSS의 제목과 본문을 매핑하였다.

**프롬프트:**

```
You are an English difficulty classifier. Analyze vocabulary and sentence complexity.
BBC World News articles are written for educated adults. Therefore:
- ALWAYS classify as Medium by default
- Only classify as High if you see technical/legal/academic terms
- NEVER classify as Low unless the text is simpler than a children's book

You MUST respond with EXACTLY ONE WORD: High, Medium, or Low.
Do not write anything else. Do not explain. Do not add punctuation. Just one word.
```

<!-- 이미지: images/4-3_난이도분류GPT.png -->
![난이도 분류 GPT 설정](images/4-3_난이도분류GPT.png)

### 4.4 ③ Router 조건 분기 설정

메인 경로에는 `Content Does not contain Low` 필터를 설정하여 Medium·High 뉴스만 통과시키고, 나머지 경로는 fallback으로 지정하였다.

<!-- 이미지: images/4-4_Router분기.png -->
![Router 조건 분기 설정](images/4-4_Router분기.png)

### 4.5 ④ 학습 콘텐츠 생성 GPT 설정

뉴스 요약, 고급 표현 5개(뜻·영영정의·예문), 복습 퀴즈 1문제를 생성하도록 프롬프트를 구성하였다. 이메일에서 깔끔하게 보이도록 HTML 형식으로 출력하게 하였다.

**프롬프트:**

```
You are an English learning assistant. Output in HTML format only.

<h2>📰 뉴스 요약</h2>
<p>[2-3문장으로 한국어 요약]</p>
<hr>
<h2>📚 오늘의 표현</h2>
<h3>1. [expression]</h3>
<p><b>뜻:</b> 한국어 의미</p>
<p><b>Definition:</b> English definition</p>
<p><b>Example:</b> example sentence</p>
(repeat for 5 expressions)
<hr>
<h2>🧠 Quiz</h2>
<p>[quiz question]</p>
<p>A) B) C) D)</p>
<p><b>정답:</b> [answer]</p>
```

<!-- 이미지: images/4-5_콘텐츠생성GPT.png -->
![학습 콘텐츠 생성 GPT 설정](images/4-5_콘텐츠생성GPT.png)

### 4.6 ⑤ Notion 저장 설정

'시사 영어 단어장' 데이터베이스에 날짜·뉴스 제목·난이도·학습 완료 속성을 매핑하여 새 페이지를 생성하도록 설정하였다.

<!-- 이미지: images/4-6_Notion저장.png -->
![Notion 저장 설정](images/4-6_Notion저장.png)

### 4.7 ⑥ Gmail 발송 설정 (메인 / fallback)

메인 경로 Gmail은 학습 콘텐츠를 HTML 본문으로 발송하고, fallback 경로 Gmail은 '오늘은 적합한 뉴스가 없다'는 안내 메일을 발송하도록 설정하였다.

<!-- 이미지: images/4-7_Gmail설정.png -->
![Gmail 발송 설정](images/4-7_Gmail설정.png)

---

## 5. 실행 결과 화면 캡처

워크플로우를 실제로 실행(Run once)한 결과이다. 조건 분기의 두 경로가 각각 실행된 결과를 모두 확인할 수 있다.

### 5.1 메인 경로 실행 결과 (Medium·High 통과)

난이도가 Medium 또는 High로 분류된 뉴스가 Router의 메인 경로(1st)를 통과한 경우이다. 통과 항목 1건이 표시되며, 이후 OpenAI 콘텐츠 생성 → Notion 저장 → Gmail 학습 알림 발송까지 전 모듈이 실행 성공(✅)으로 표시된다.

<!-- 이미지: images/5-1_메인경로결과.png -->
![메인 경로 실행 결과](images/5-1_메인경로결과.png)

*▲ 메인 경로 실행: Medium·High 통과 1건 → OpenAI·Notion·Gmail 모두 실행 성공*

### 5.2 fallback 경로 실행 결과 (Low 처리)

Medium·High 뉴스가 한 건도 없어 메인 경로 통과가 0건인 경우이다. 이때 fallback(2nd) 경로가 실행되어 Gmail 스킵 알림이 발송된다.

<!-- 이미지: images/5-2_fallback경로결과.png -->
![fallback 경로 실행 결과](images/5-2_fallback경로결과.png)

*▲ fallback 경로 실행: 메인 경로 통과 0건 → fallback Gmail 발송 성공*

> **위 5.1과 5.2를 통해 조건 분기의 두 경로(메인·fallback)가 각각 1회 이상 실제로 실행되었음을 확인할 수 있다.**

### 5.3 Notion 저장 결과

메인 경로 실행 시 Notion 단어장 데이터베이스에 새 항목이 자동으로 생성되고, 본문에 학습 콘텐츠가 저장된 화면이다.

<!-- 이미지: images/5-3_Notion저장결과.png -->
![Notion 저장 결과](images/5-3_Notion저장결과.png)

### 5.4 Gmail 수신 결과

실제로 수신한 학습 알림 이메일 화면이다. 뉴스 요약, 고급 표현, 예문, 복습 퀴즈가 포함되어 있다.

<!-- 이미지: images/5-4_Gmail수신결과.png -->
![Gmail 수신 결과](images/5-4_Gmail수신결과.png)

---

## 6. 결론 및 기대 효과

본 프로젝트를 통해 매일 20~30분이 소요되던 영어 뉴스 학습 준비 과정을 완전히 자동화하였다. 사용자는 더 이상 뉴스를 직접 찾고 정리할 필요 없이, 매일 아침 도착하는 이메일과 Notion 페이지로 학습에만 집중할 수 있다.

특히 GPT를 활용한 난이도 자동 분류와 Router 기반 조건 분기를 통해, 학습 가치가 높은 뉴스는 단어장으로 가공하고 적합한 뉴스가 없는 날에는 별도 안내 메일을 보내는 **'맞춤형' 구조**를 구현하였다. 두 분기 경로가 상황에 맞게 각각 동작하도록 설계하여, 어떤 경우에도 사용자가 결과를 받아볼 수 있다.

향후에는 난이도 분류 기준을 사용자 수준에 맞게 세분화하거나, 학습 완료율을 집계하여 주간 리포트를 발송하는 기능 등으로 확장할 수 있다.

# 워크플로우 자동화 심화 보고서
### Trigger와 Action, 오류 처리, 워크플로우 모듈화, 노코드의 한계와 확장

---

## 들어가며

본 보고서는 노코드 자동화 도구(Zapier, Make, n8n, Power Automate 등)를 기반으로 한 워크플로우 설계 역량을 점검하기 위한 네 가지 평가 질문에 대한 답변을 정리한 것이다. 단순한 도구 사용법을 넘어, **자동화의 작동 원리**, **장애 상황에서의 견고함(robustness)**, **규모가 커질 때의 구조 설계**, 그리고 **노코드의 경계와 그 너머의 확장**까지를 다룬다.

---

## 1. Trigger와 Action의 차이 — 왜 Trigger는 '시작점'이고 Action은 '처리'인가

### 1.1 개념적 정의

| 구분 | Trigger (트리거) | Action (액션) |
|------|------------------|----------------|
| 역할 | 워크플로우를 **시작시키는 사건(event)** | 시작된 흐름 안에서 **수행되는 작업(task)** |
| 발생 방식 | 외부/내부에서 **발생을 기다림(수동적 감지)** | 트리거 이후 **능동적으로 실행됨** |
| 개수 | 워크플로우당 **보통 1개** | **0개 이상, 여러 개 연쇄 가능** |
| 비유 | 도미노의 **첫 번째 패** | 그 뒤로 **넘어지는 모든 패** |
| 데이터 관점 | 데이터를 **만들어 내보냄(produce)** | 데이터를 **받아 가공/전달(consume)** |

### 1.2 핵심 차이: '감지'와 '실행'

Trigger와 Action을 가르는 본질적 기준은 **누가 흐름의 발생 시점을 결정하는가**이다.

- **Trigger는 "언제 시작할지"를 결정한다.** 트리거는 스스로 무언가를 만들어내지 않고, 정해둔 조건의 사건이 **외부에서 발생하기를 기다린다.** 새 이메일 도착, 폼 제출, 특정 시각 도래, DB에 행 추가 — 이 사건들은 워크플로우가 통제할 수 없는 외부 세계에서 일어난다. 그래서 트리거는 흐름의 **입구이자 시작점**이다. 트리거가 울리지 않으면 그 뒤의 어떤 것도 실행되지 않는다.

- **Action은 "무엇을 할지"를 수행한다.** 액션은 트리거가 이미 흐름을 열어준 **이후에야** 동작한다. 트리거가 넘겨준 데이터를 입력으로 받아 변환하고, 저장하고, 외부 시스템으로 보낸다. 액션은 시작 시점을 스스로 정할 수 없으며, 항상 **앞선 사건의 결과로서 실행**된다. 그래서 액션은 시작점이 아니라 **처리(processing)** 단계다.

### 1.3 구체적 예시

**예시 A — 고객 문의 자동 분류**

```
[Trigger] 고객지원 이메일함에 새 메일 도착
   ↓
[Action 1] 메일 본문을 텍스트로 추출
   ↓
[Action 2] 키워드 기반으로 카테고리 분류 (환불 / 배송 / 기타)
   ↓
[Action 3] 분류 결과를 Slack 채널에 알림 전송
   ↓
[Action 4] 스프레드시트에 문의 로그 1행 추가
```

여기서 **메일 도착**은 우리가 통제하지 못하는 외부 사건이므로 시작점(Trigger)이다. 추출·분류·알림·기록은 모두 그 사건이 발생한 **뒤에** 순차적으로 수행되는 처리이므로 Action이다.

**예시 B — 시간 기반 자동화**

```
[Trigger] 매일 오전 9시 (스케줄)
   ↓
[Action 1] 어제 매출 데이터를 DB에서 조회
   ↓
[Action 2] 요약 리포트 생성
   ↓
[Action 3] 팀 이메일로 발송
```

여기서는 **"오전 9시가 됨"**이라는 시간 사건이 트리거다. 시간이 흐르는 것은 워크플로우가 만드는 것이 아니라 기다리는 대상이다. 따라서 동일한 작업(조회·생성·발송)이라도 그것을 **촉발하는 사건**이 무엇이냐에 따라 트리거가 되고, 그 뒤의 일은 액션이 된다.

### 1.4 정리

> **Trigger는 흐름이 "시작되는 조건"을 정의하고, Action은 시작된 흐름 안에서 "수행되는 일"을 정의한다.**
> 같은 "Slack 메시지 전송"이라도, 그것이 흐름을 **여는** 사건이면(예: Slack에 메시지가 올라오면) 트리거이고, 흐름 **안에서 보내는** 작업이면 액션이다. 즉 본질은 기능의 종류가 아니라 **흐름 상의 위치 — 발생을 기다리는가, 발생 이후 실행되는가**에 있다.

---

## 2. 트리거 불안정·외부 연동 실패 시 — 모니터링·재시도·대체 경로 설계

외부 API나 웹훅(webhook)에 의존하는 트리거는 본질적으로 불안정하다. 외부 서비스 다운, 네트워크 지연, 인증 토큰 만료, 레이트 리밋(rate limit), 웹훅 유실 등이 언제든 발생할 수 있다. 견고한 자동화는 "실패하지 않는 것"이 아니라 **"실패를 전제로 설계하는 것"**이다.

### 2.1 모니터링 (Monitoring) — 실패를 '알 수 있게' 만들기

자동화의 가장 큰 위험은 실패 자체가 아니라 **실패를 모르는 것**(silent failure)이다.

1. **헬스 체크(Heartbeat / Dead Man's Switch)**
   주기적으로 실행되어야 하는 트리거(예: 매시간 폴링)는 "정상 실행"을 외부 모니터링 서비스(예: Healthchecks.io, Cronitor)에 ping으로 보고하도록 한다. 정해진 시간 내에 ping이 오지 않으면 "트리거가 멈췄다"는 알림이 발송된다. 즉 **실행되지 않은 것**까지 감지한다.

2. **실행 로그 적재**
   모든 실행의 시작/종료/상태(성공·실패·건너뜀)와 입력 식별자를 별도 시트나 DB, 로깅 서비스(Datadog, Sentry 등)에 기록한다. 사후 추적과 통계(실패율, 지연 시간) 분석의 근거가 된다.

3. **에러 알림 채널 분리**
   실패 알림은 일반 업무 채널이 아니라 **전용 알림 채널(#automation-alerts)**로 보내고, 심각도(severity)를 구분한다. 단순 재시도로 복구된 일시 오류는 로그만, 최종 실패는 담당자 멘션과 함께 즉시 통지한다.

### 2.2 재시도 (Retry) — 일시적 오류를 자동 복구

외부 연동 실패의 상당수는 **일시적(transient)**이다. 잠시 후 다시 시도하면 성공한다.

1. **지수 백오프(Exponential Backoff)**
   즉시 재시도하면 부하가 겹쳐 오히려 악화된다. 재시도 간격을 1초 → 2초 → 4초 → 8초처럼 점점 늘린다. 여기에 약간의 무작위 지연(jitter)을 더해 동시 재시도가 한꺼번에 몰리는 것을 방지한다.

2. **재시도 횟수 상한 + 오류 분류**
   무한 재시도는 금물이다. 보통 3~5회로 제한한다. 또한 **재시도해야 할 오류**(5xx 서버 오류, 타임아웃, 429 레이트 리밋)와 **재시도해도 소용없는 오류**(401 인증 실패, 400 잘못된 요청)를 구분한다. 후자는 즉시 멈추고 사람에게 알린다.

3. **멱등성(Idempotency) 확보**
   재시도가 같은 작업을 **두 번 실행**해 중복(중복 결제, 중복 메일)을 일으키면 안 된다. 각 처리에 고유 키(idempotency key)를 부여하거나, "이미 처리됨" 여부를 먼저 확인한 뒤 실행한다.

### 2.3 대체 경로 (Fallback) — 그래도 안 될 때

재시도로도 복구되지 않을 때를 위한 우회로를 미리 설계한다.

1. **데드 레터 큐(Dead Letter Queue, DLQ)**
   최종 실패한 작업의 **입력 데이터 전체**를 별도 저장소(전용 시트·테이블·큐)에 보관한다. 외부 서비스가 복구된 뒤 이 큐를 다시 흘려보내(replay) 누락 없이 처리할 수 있다. 데이터 유실을 막는 핵심 안전망이다.

2. **대체 트리거 / 다중화**
   웹훅(push)이 유실될 수 있는 환경에서는 **주기적 폴링(pull)**을 보조 트리거로 함께 둔다. 웹훅을 놓친 건이 있어도 다음 폴링에서 잡아낸다. 채널·서비스를 이중화(예: 이메일 발송 실패 시 SMS)하는 것도 같은 원리다.

3. **수동 개입 경로(Human-in-the-loop)**
   자동 복구가 불가능한 건은 담당자에게 "검토 필요" 작업으로 넘긴다. 자동화가 **조용히 실패하는 대신, 명시적으로 사람에게 에스컬레이션**하도록 만드는 것이 안전하다.

### 2.4 설계 요약 다이어그램

```
[외부 이벤트] → [Trigger]
                   │ 실패?
        ┌──────────┴──────────┐
       성공                  실패
        │                     │
     [정상 처리]        [지수 백오프 재시도 ≤ 3회]
                              │ 여전히 실패?
                   ┌──────────┴──────────┐
              일시적 오류             영구적 오류
                   │                     │
            [폴링 대체경로]      [DLQ 저장 + 담당자 알림]
                                       (수동 개입)

  ── 전 과정에 걸쳐: Heartbeat 모니터링 + 실행 로그 적재 ──
```

---

## 3. 분기·로직이 복잡해질 때 — 워크플로우 구조화와 모듈화

조건 분기가 2개에서 5개로 늘고 처리 로직이 복잡해지면, 하나의 거대한 단일 워크플로우는 **가독성·디버깅·재사용성**이 급격히 나빠진다. 이를 다루는 핵심 원칙은 소프트웨어 공학의 **모듈화·관심사 분리·DRY(중복 제거)**를 노코드에 적용하는 것이다.

### 3.1 문제: '스파게티 워크플로우'

분기가 많아지면 한 캔버스 안에서 화살표가 거미줄처럼 얽힌다. 어디서 무엇이 잘못됐는지 추적하기 어렵고, 한 군데 수정이 다른 분기를 깨뜨린다. 이는 코드의 "갓 함수(God function)"와 같은 안티패턴이다.

### 3.2 해법 1 — 서브 워크플로우로 분리 (모듈화)

반복되거나 독립적인 처리 단위를 **별도의 서브 워크플로우(sub-workflow)**로 떼어낸다. 메인 워크플로우는 이들을 **호출(call)**만 한다.

- 예: "고객 알림 보내기" 로직(이메일·SMS·Slack 채널 선택 포함)을 하나의 모듈로 분리하면, 환불·배송·가입 등 어느 분기에서든 **동일 모듈을 재사용**할 수 있다.
- 장점: 알림 로직이 바뀌면 **한 곳만** 수정하면 된다(DRY). 각 모듈을 **독립적으로 테스트**할 수 있다.
- 도구별 지원: n8n의 *Execute Workflow*, Make의 *시나리오 분리 + 웹훅 호출*, Zapier의 *Sub-Zap / Webhook 연계* 등.

### 3.3 해법 2 — 라우터/스위치로 분기 정리

if 노드를 5단으로 중첩하지 말고, **다지선다 라우터(Router/Switch)** 노드 하나로 평탄화한다.

```
                 ┌─ case "환불"  → [환불 처리 모듈]
[Router: 분류값] ─┼─ case "배송"  → [배송 처리 모듈]
                 ├─ case "교환"  → [교환 처리 모듈]
                 ├─ case "가입"  → [가입 처리 모듈]
                 └─ default      → [기타/수동 검토 모듈]
```

- 중첩 if의 깊이를 없애 **한눈에 분기 전체**를 본다.
- 각 case의 처리부를 모듈로 두면, 분기 추가는 "case 한 줄 + 모듈 하나 추가"로 끝난다(확장 용이).

### 3.4 해법 3 — 관심사 분리 (계층 구조)

워크플로우를 역할별 계층으로 나눈다.

1. **수집(Ingestion) 계층** — 트리거 + 입력 정규화(데이터 모양을 통일)
2. **판단(Routing) 계층** — 분류·분기 결정만 담당
3. **처리(Processing) 계층** — 분기별 실제 작업(각각 모듈)
4. **출력(Output) 계층** — 알림·기록·후속 작업

각 계층이 **하나의 책임만** 지므로, 새 분기 추가는 처리 계층의 모듈 추가로 국한되고 다른 계층은 건드리지 않는다.

### 3.5 해법 4 — 설정의 외부화(Configuration over Logic)

분기 규칙이 자주 바뀐다면, 규칙을 워크플로우 **로직에 하드코딩하지 말고** 외부 테이블(스프레드시트·DB)에 둔다.

- 예: "키워드 → 카테고리 → 담당팀" 매핑을 시트로 관리. 새 규칙이 생기면 **워크플로우를 수정하지 않고 시트 행만 추가**한다.
- 로직(엔진)과 데이터(규칙)를 분리하면, 분기가 5개든 50개든 워크플로우 구조 자체는 그대로 유지된다. 이는 가장 강력한 확장 전략이다.

### 3.6 운영 원칙

| 원칙 | 적용 |
|------|------|
| 단일 책임 | 한 워크플로우/모듈은 한 가지 일만 |
| DRY | 반복 로직은 모듈로 추출해 재사용 |
| 명확한 명명 | 모듈·노드 이름으로 의도를 드러냄 |
| 버전 관리 | 변경 이력 보존, 가능 시 export로 백업 |
| 설정 외부화 | 자주 바뀌는 규칙은 데이터로 분리 |

---

## 4. 노코드 자동화의 한계 1가지와 코딩 기반 자동화로의 확장

### 4.1 선정한 한계 업무 — "복잡한 비정형 데이터의 조건부 변환·정합 처리"

**구체적 시나리오:** 매일 수십 개 거래처에서 **서로 형식이 제각각인** 엑셀/CSV 정산 파일이 들어온다. 어떤 곳은 날짜가 `2026-01-05`, 어떤 곳은 `5/1/26`, 또 어떤 곳은 한글 `1월 5일`로 적혀 있다. 금액 표기, 컬럼 순서, 통화 단위, 빈 셀 처리 규칙도 거래처마다 다르다. 이를 하나의 표준 스키마로 정규화하고, 내부 회계 데이터와 **대사(reconciliation, 금액·건수 대조)**하여 불일치를 찾아내야 한다.

### 4.2 왜 노코드로는 한계인가

1. **복잡한 조건·예외 처리의 폭발**
   거래처별 변환 규칙이 수십 가지로 갈라지고 서로 중첩된다. 노코드의 시각적 분기/포뮬러로 표현하면 캔버스가 통제 불능으로 비대해지고, 미묘한 예외("월·일이 뒤바뀐 날짜 추정")는 규칙으로 깔끔히 떨어지지 않는다.

2. **고급 데이터 변환·알고리즘의 부재**
   퍼지 매칭(유사 문자열로 거래처명 대조), 가중치 기반 대사, 통계적 이상치 탐지 같은 처리는 노코드 내장 블록만으로는 사실상 불가능하거나 극도로 비효율적이다.

3. **성능과 대용량 처리**
   수십만 행을 행 단위로 순회하는 노코드 루프는 느리고, 실행 시간·연산 횟수(operation) 과금 한도에 쉽게 부딪힌다.

4. **테스트·재현성·버전 관리의 취약함**
   복잡한 변환 로직은 단위 테스트로 검증해야 신뢰할 수 있는데, 노코드는 자동화된 회귀 테스트와 코드 리뷰, 정교한 버전 관리가 어렵다.

> 정리하면, 노코드는 **"이벤트를 연결하고 표준 작업을 오케스트레이션"**하는 데 탁월하지만, **"복잡하고 비정형적인 데이터를 알고리즘적으로 변환·판단"**하는 영역에서는 표현력·성능·검증 가능성이 모두 부족하다.

### 4.3 코딩 기반 자동화로의 확장 아이디어

핵심 전략은 **노코드를 버리는 것이 아니라**, 복잡한 핵심 로직만 코드로 떼어내고 노코드가 그것을 **오케스트레이션**하게 하는 **하이브리드 구조**다.

1. **"커스텀 코드 블록" 임베딩 (가장 가벼운 확장)**
   대부분의 노코드 도구는 코드 실행 노드를 제공한다(n8n의 *Code/Function* 노드, Make의 *Custom JS/Functions*, Zapier의 *Code by Zapier*, Power Automate의 *인라인 스크립트*). 날짜·금액 정규화 같은 까다로운 변환만 이 노드 안의 JavaScript/Python으로 처리하고, 나머지 흐름은 노코드로 유지한다.

2. **마이크로서비스/서버리스 함수로 핵심 로직 분리**
   퍼지 매칭·대사 알고리즘을 별도 서비스(AWS Lambda, Google Cloud Functions, 또는 사내 API)로 구현한다. 노코드 워크플로우는 파일이 도착하면 이 함수의 **API를 호출(HTTP Request 액션)**하고, 표준화·대사된 결과만 받아 후속 작업(저장·알림)을 이어간다. **무거운 계산은 코드, 흐름 연결은 노코드**라는 역할 분담이 명확해진다.

3. **전용 파이프라인으로의 졸업(Graduation)**
   데이터 양과 로직이 더 커지면, 핵심 처리를 본격적인 코드 파이프라인(Python + pandas/Polars, dbt, Airflow/Dagster 등 워크플로우 오케스트레이터)으로 옮긴다. 이때도 노코드는 **트리거·알림·사람 대상 인터페이스** 같은 "가장자리"에 남겨 두면, 개발 속도와 견고함을 모두 얻는다.

### 4.4 확장 구조 다이어그램

```
[노코드 영역]                        [코드 영역]
새 파일 도착(Trigger)
      │
파일 수집·전처리(Action)
      │
      └── HTTP 호출 ──────────────▶ [정규화·퍼지매칭·대사 API]
                                          (Python/서버리스)
      ┌── 표준화·대사 결과 수신 ◀────────────┘
      │
불일치 알림 / 시트 기록(Action)
정상 건 후속 처리(Action)
```

이 구조에서 **노코드는 "지휘자(orchestrator)", 코드는 "전문 연주자(specialist)"** 역할을 맡는다. 각자 가장 잘하는 일에 집중함으로써, 노코드의 빠른 개발 속도와 코드의 표현력·성능·검증 가능성을 동시에 확보하는 것이 확장의 핵심 아이디어다.

---

## 맺으며

네 가지 질문은 결국 하나의 흐름으로 이어진다. **(1) 트리거와 액션**이라는 자동화의 기본 단위를 정확히 이해하고, **(2) 외부 의존성의 실패를 전제로 견고하게** 만들며, **(3) 규모가 커져도 무너지지 않도록 모듈화**하고, **(4) 노코드의 경계에 다다랐을 때 코드로 확장**하는 것 — 이것이 단순히 도구를 "쓰는" 수준을 넘어 자동화를 **설계하고 운영하는** 역량이다.


